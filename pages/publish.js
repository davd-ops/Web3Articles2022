import React from 'react';
import Meta from "../components/Meta";

const Publish = () => {
    return (
        <div>
            <Meta title='Publish' />
            <h1>Publish</h1>
        </div>
    );
};

export default Publish;
